<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mis Productos</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link rel="stylesheet" href="/stylesheets/productStyles.css" />
</head>
<body>
    <%- include('../partials/header') %>

    <div class="cart-container">
        <h1>Mis Productos</h1>
        <ul>
            <% products.forEach(product => { %>
                <li class="product-overview">
                    <div class="product-image">
                        <img src="<%= product.image %>" alt="<%= product.name %>">
                    </div>
                    <div class="product-details">
                        <h2><%= product.name %></h2>
                        <p class="product-description"><%= product.description %></p>
                        <p class="product-price">Precio: $<%= product.price %></p>
                        <a href="/products/productDetails/<%= product.id %>">Ver Detalle</a> |
                        <a href="/products/<%= product.id %>/edit">Editar</a> |
                        <form action="/products/<%= product.id %>?_method=DELETE" method="POST" style="display:inline;">
                            <button class="buy-button" type="submit">Eliminar</button>
                        </form>
                    </div>
                </li>
            <% }) %>
        </ul>
    </div>

    <%- include('../partials/footer', { user: user }) %>
</body>
</html>
